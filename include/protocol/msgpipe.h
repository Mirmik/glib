#ifndef MSG_SOCKET_H
#define MSG_SOCKET_H

template<typename socktype>
class msg_over_socket
{
	socktype* s;

	void send(char* buf, int len)
	{

	};

	void recv(char* buf, int len)
	{

	};	
};

#endif